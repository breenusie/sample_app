<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages_micro' %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Say sumpthin..." %>
  </div>
  <%= f.submit "Post", class: "btn btn-large btn-primary" %>
  <span class="countdown"></span>
<% end %>
